# Seed data notes

Status: no seed data loaded. CSV placeholders have headers only.

When Stage 2 completes, this file will record:

- How `data/*.csv` were generated
- Exact row counts (including duplicate extras)
- Injected issue counts vs the mandatory list
- That Bronze was seeded from those files without transformation
- Confirmation that data is synthetic

Until then, do not assume Databricks tables contain 10,000 / 100,000 / 500 rows.
